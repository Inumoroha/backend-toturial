# 6. 综合实践：重构用户模块

本节目标：把前面阶段的用户 CRUD 重构成完整分层结构。完成后，你的项目应该不再把所有代码堆在 `main.go`，而是形成清晰的 model、repository、service、handler、router、response 层。

---

## 一、最终目标

重构后，你应该拥有类似结构：

```text
gin-layer-demo/
  cmd/
    server/
      main.go
  internal/
    model/
      user.go
    repository/
      user_repository.go
    service/
      user_service.go
    handler/
      user_handler.go
    response/
      response.go
      code.go
      validation.go
    router/
      router.go
  go.mod
  requests.http
```

最终用户接口保持不变：

```text
GET    /api/v1/users
POST   /api/v1/users
GET    /api/v1/users/:id
PUT    /api/v1/users/:id
PATCH  /api/v1/users/:id/status
DELETE /api/v1/users/:id
```

重构的要求是：

```text
接口行为不变，代码结构变清晰。
```

---

## 二、第一步：创建目录

```bash
mkdir -p cmd/server
mkdir -p internal/model
mkdir -p internal/repository
mkdir -p internal/service
mkdir -p internal/handler
mkdir -p internal/response
mkdir -p internal/router
```

PowerShell：

```powershell
$dirs = @(
  "cmd/server",
  "internal/model",
  "internal/repository",
  "internal/service",
  "internal/handler",
  "internal/response",
  "internal/router"
)
foreach ($d in $dirs) { New-Item -ItemType Directory -Force -Path $d }
```

---

## 三、第二步：移动 User model

`internal/model/user.go`：

```go
package model

import "time"

type User struct {
    ID        uint      `json:"id"`
    Username  string    `json:"username"`
    Email     string    `json:"email"`
    Age       int       `json:"age"`
    Status    string    `json:"status"`
    CreatedAt time.Time `json:"created_at"`
}
```

检查点：

```text
main.go 中不再定义 User。
其他地方使用 model.User。
```

---

## 四、第三步：移动 response

`internal/response/code.go`：

```go
package response

const (
    CodeOK             = 0
    CodeInvalidRequest = 40001
    CodeNotFound       = 40401
    CodeConflict       = 40901
    CodeInternalError  = 50001
)
```

`internal/response/response.go`：

```go
package response

import (
    "net/http"

    "github.com/gin-gonic/gin"
)

type Body struct {
    Code    int         `json:"code"`
    Message string      `json:"message"`
    Data    interface{} `json:"data,omitempty"`
}

type PageResult struct {
    Items    interface{} `json:"items"`
    Total    int64       `json:"total"`
    Page     int         `json:"page"`
    PageSize int         `json:"page_size"`
}

func OK(c *gin.Context, data interface{}) {
    c.JSON(http.StatusOK, Body{Code: CodeOK, Message: "ok", Data: data})
}

func Created(c *gin.Context, data interface{}) {
    c.JSON(http.StatusCreated, Body{Code: CodeOK, Message: "ok", Data: data})
}

func Fail(c *gin.Context, status int, code int, message string) {
    c.JSON(status, Body{Code: code, Message: message})
}
```

检查点：

```text
handler 中不再到处手写 gin.H 响应格式。
```

---

## 五、第四步：实现 repository

`internal/repository/user_repository.go`：

```go
package repository

import (
    "errors"
    "strings"
    "time"

    "gin-layer-demo/internal/model"
)

var ErrUserNotFound = errors.New("user not found")
var ErrEmailAlreadyExists = errors.New("email already exists")

type UserRepository struct {
    users  map[uint]model.User
    nextID uint
}

func NewUserRepository() *UserRepository {
    return &UserRepository{
        users: map[uint]model.User{
            1: {ID: 1, Username: "alice", Email: "alice@example.com", Age: 20, Status: "active", CreatedAt: time.Now()},
            2: {ID: 2, Username: "bob", Email: "bob@example.com", Age: 22, Status: "disabled", CreatedAt: time.Now()},
        },
        nextID: 3,
    }
}
```

补充方法：

```go
func (r *UserRepository) GetByID(id uint) (model.User, error) {
    user, ok := r.users[id]
    if !ok {
        return model.User{}, ErrUserNotFound
    }
    return user, nil
}

func (r *UserRepository) Create(user model.User) (model.User, error) {
    for _, item := range r.users {
        if item.Email == user.Email {
            return model.User{}, ErrEmailAlreadyExists
        }
    }

    user.ID = r.nextID
    user.CreatedAt = time.Now()
    r.users[user.ID] = user
    r.nextID++
    return user, nil
}

func (r *UserRepository) List(status, keyword string) []model.User {
    result := make([]model.User, 0)
    keyword = strings.ToLower(keyword)

    for _, user := range r.users {
        if status != "" && user.Status != status {
            continue
        }
        if keyword != "" {
            username := strings.ToLower(user.Username)
            email := strings.ToLower(user.Email)
            if !strings.Contains(username, keyword) && !strings.Contains(email, keyword) {
                continue
            }
        }
        result = append(result, user)
    }
    return result
}

func (r *UserRepository) Update(user model.User) (model.User, error) {
    if _, ok := r.users[user.ID]; !ok {
        return model.User{}, ErrUserNotFound
    }

    for _, item := range r.users {
        if item.Email == user.Email && item.ID != user.ID {
            return model.User{}, ErrEmailAlreadyExists
        }
    }

    r.users[user.ID] = user
    return user, nil
}

func (r *UserRepository) Delete(id uint) error {
    if _, ok := r.users[id]; !ok {
        return ErrUserNotFound
    }
    delete(r.users, id)
    return nil
}
```

检查点：

```text
只有 repository 直接访问 users map。
```

---

## 六、第五步：实现 service

`internal/service/user_service.go`：

```go
package service

import (
    "errors"

    "gin-layer-demo/internal/model"
    "gin-layer-demo/internal/repository"
)

var ErrUserNotFound = errors.New("user not found")
var ErrEmailAlreadyExists = errors.New("email already exists")
var ErrInvalidUserStatus = errors.New("invalid user status")

type UserService struct {
    repo *repository.UserRepository
}

func NewUserService(repo *repository.UserRepository) *UserService {
    return &UserService{repo: repo}
}
```

input：

```go
type CreateUserInput struct {
    Username string
    Email    string
    Age      int
    Status   string
}

type UpdateUserInput struct {
    ID       uint
    Username string
    Email    string
    Age      int
    Status   string
}
```

方法：

```go
func (s *UserService) Create(input CreateUserInput) (model.User, error) {
    status := input.Status
    if status == "" {
        status = "active"
    }
    if !isValidStatus(status) {
        return model.User{}, ErrInvalidUserStatus
    }

    user, err := s.repo.Create(model.User{
        Username: input.Username,
        Email:    input.Email,
        Age:      input.Age,
        Status:   status,
    })
    if errors.Is(err, repository.ErrEmailAlreadyExists) {
        return model.User{}, ErrEmailAlreadyExists
    }
    return user, err
}

func (s *UserService) Get(id uint) (model.User, error) {
    user, err := s.repo.GetByID(id)
    if errors.Is(err, repository.ErrUserNotFound) {
        return model.User{}, ErrUserNotFound
    }
    return user, err
}

func isValidStatus(status string) bool {
    return status == "active" || status == "disabled"
}
```

检查点：

```text
默认 status=active 放在 service。
service 不导入 gin。
```

---

## 七、第六步：实现 handler

`internal/handler/user_handler.go`：

```go
package handler

import (
    "errors"
    "net/http"
    "strconv"

    "gin-layer-demo/internal/response"
    "gin-layer-demo/internal/service"

    "github.com/gin-gonic/gin"
)

type UserHandler struct {
    service *service.UserService
}

func NewUserHandler(service *service.UserService) *UserHandler {
    return &UserHandler{service: service}
}
```

请求 DTO：

```go
type CreateUserRequest struct {
    Username string `json:"username" binding:"required,min=2,max=32"`
    Email    string `json:"email" binding:"required,email"`
    Age      int    `json:"age" binding:"gte=1,lte=120"`
    Status   string `json:"status" binding:"omitempty,oneof=active disabled"`
}
```

创建接口：

```go
func (h *UserHandler) Create(c *gin.Context) {
    var req CreateUserRequest
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Fail(c, http.StatusBadRequest, response.CodeInvalidRequest, "invalid request")
        return
    }

    user, err := h.service.Create(service.CreateUserInput{
        Username: req.Username,
        Email:    req.Email,
        Age:      req.Age,
        Status:   req.Status,
    })
    if errors.Is(err, service.ErrEmailAlreadyExists) {
        response.Fail(c, http.StatusConflict, response.CodeConflict, "email already exists")
        return
    }
    if errors.Is(err, service.ErrInvalidUserStatus) {
        response.Fail(c, http.StatusBadRequest, response.CodeInvalidRequest, "invalid status")
        return
    }
    if err != nil {
        response.Fail(c, http.StatusInternalServerError, response.CodeInternalError, "internal server error")
        return
    }

    response.Created(c, user)
}
```

检查点：

```text
handler 只处理 HTTP。
handler 调 service，不直接访问 repository。
```

---

## 八、第七步：实现 router 和 main

`internal/router/router.go`：

```go
package router

import (
    "gin-layer-demo/internal/handler"

    "github.com/gin-gonic/gin"
)

func New(userHandler *handler.UserHandler) *gin.Engine {
    r := gin.Default()

    api := r.Group("/api/v1")
    {
        api.POST("/users", userHandler.Create)
        api.GET("/users/:id", userHandler.Get)
    }

    return r
}
```

`cmd/server/main.go`：

```go
package main

import (
    "log"

    "gin-layer-demo/internal/handler"
    "gin-layer-demo/internal/repository"
    "gin-layer-demo/internal/router"
    "gin-layer-demo/internal/service"
)

func main() {
    userRepo := repository.NewUserRepository()
    userService := service.NewUserService(userRepo)
    userHandler := handler.NewUserHandler(userService)

    r := router.New(userHandler)

    if err := r.Run(":8080"); err != nil {
        log.Fatal(err)
    }
}
```

---

## 九、最终验收

运行：

```bash
go run ./cmd/server
```

测试：

```http
POST http://localhost:8080/api/v1/users
Content-Type: application/json

{
  "username": "cindy",
  "email": "cindy@example.com",
  "age": 21
}
```

再测试：

```http
GET http://localhost:8080/api/v1/users/1
```

---

## 十、复盘问题

完成后请检查：

- `main.go` 是否只负责组装？
- handler 是否还直接访问 map？
- service 是否导入了 Gin？
- repository 是否返回了 HTTP 状态码？
- response 是否统一？
- model 是否被多个层复用？

如果答案都符合预期，说明用户模块已经完成第一轮分层重构。


