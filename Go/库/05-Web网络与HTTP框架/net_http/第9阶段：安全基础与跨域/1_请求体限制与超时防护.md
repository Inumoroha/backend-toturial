# 1. 请求体限制与超时防护

本节目标：理解请求体大小限制和 Server 超时为什么是安全基础。

---

## 一、为什么要限制请求体

如果服务端无限制读取请求体，恶意客户端可以发送特别大的 Body，占用：

- 内存。
- 磁盘临时文件。
- 网络带宽。
- goroutine 时间。

JSON API 也应该限制大小。

---

## 二、使用 MaxBytesReader

```go
r.Body = http.MaxBytesReader(w, r.Body, 1<<20) // 1MB
```

放在读取 Body 前：

```go
func createTodo(w http.ResponseWriter, r *http.Request) {
	r.Body = http.MaxBytesReader(w, r.Body, 1<<20)

	var req createTodoRequest
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeError(w, http.StatusBadRequest, "invalid_json", "invalid json body")
		return
	}
}
```

---

## 三、Server 超时也是安全配置

```go
srv := &http.Server{
	ReadHeaderTimeout: 5 * time.Second,
	ReadTimeout:       10 * time.Second,
	WriteTimeout:      10 * time.Second,
	IdleTimeout:       60 * time.Second,
}
```

这些配置能减少慢连接、慢请求、慢响应带来的资源占用。

---

## 四、不同接口可以有不同策略

普通 JSON API：

```text
Body 限制：1MB
ReadTimeout：10s
```

文件上传接口：

```text
Body 限制：5MB 或更高
ReadTimeout：根据上传大小调整
```

不要所有接口都用同一个大小和超时策略。

---

## 五、本节检查点

请确认你能回答：

- 不限制请求体有什么风险？
- `MaxBytesReader` 应该放在哪里？
- Server 超时为什么和安全有关？
- 文件上传接口和 JSON API 的限制为什么可能不同？

